# Directus Environment Variables
# Copy this file to shared.env and {env}.env files
#
# Usage:
#   cp env.example shared.env   # Shared across all environments
#   cp env.example dev.env      # Development-specific
#   cp env.example prod.env     # Production-specific
#
# Variable precedence: shared.env -> {env}.env -> hostsolo.yaml environment

# ============================================
# SHARED VALUES (put in shared.env)
# These are typically the same across environments
# ============================================

# Database configuration
DB_CLIENT=sqlite3
DB_FILENAME=/directus/database/data.db

# Timezone
TZ=UTC

# Cache settings (optional)
# CACHE_ENABLED=true
# CACHE_TTL=30m

# ============================================
# ENVIRONMENT-SPECIFIC VALUES (put in {env}.env)
# These should be different per environment
# ============================================

# Security keys (REQUIRED - generate unique values!)
# Generate with: openssl rand -hex 32
KEY=your-unique-key-here
SECRET=your-unique-secret-here

# Admin account (for first-time setup)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-immediately

# Public URL (auto-set via Traefik labels, but can override)
# PUBLIC_URL=https://example.com
